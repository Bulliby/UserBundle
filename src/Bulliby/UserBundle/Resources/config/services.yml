parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false

    Bulliby\UserBundle\:
        resource: '../../*'
        exclude: '../../{Entity,Repository,Tests,Controller,DependencyInjection,Event,Form,Resources}'
